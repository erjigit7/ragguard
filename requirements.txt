# torch: install separately with CUDA support first, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# (plain `pip install torch` gives a CPU-only build on Windows)
sentence-transformers==5.6.0
transformers==5.13.0
accelerate==1.14.0
datasets==5.0.0
pandas==3.0.3
scikit-learn==1.9.0
fastapi==0.139.0
uvicorn==0.50.2
httpx==0.28.1
pytest==9.1.1
python-dotenv==1.2.2
